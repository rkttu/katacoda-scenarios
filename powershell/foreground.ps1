#!/usr/bin/env pwsh

Write-Host 'This is a bash shell script.\n\n'
Write-Host 'Any commands you put in here will be executed at runtime.\n\n'
Write-Host 'As such, this is a good place to install and configure anything you want to have ready for the learner before they get started.\n\n'
Write-Host 'The file is named foreground.sh because when it executes, the commands are echoed to the terminal.\n\n'
Write-Host 'To run a script in the background, so learners will not see it execute, specify it in the index.json as "background": "background.sh"\n\n'
